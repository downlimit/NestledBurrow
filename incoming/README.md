# Asset inbox

Temporary browser-upload landing area for user-provided binary assets.

Files placed here are not runtime assets. The Lead verifies their exact bytes, reuses the existing Git blob in a canonical path on `main`, then removes or ignores the inbox copy.
