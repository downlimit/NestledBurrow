# Wild Atoll standalone prototype

Open `index.html` directly in a browser.

This is a dependency-free design prototype for route topology, slot pressure, needs-driven events and expedition decisions. It is intentionally separate from the Phaser runtime. The canonical product contract is `systems/wild-atoll.md`; values and probabilities inside the HTML are experimental.
