<!-- audience: project-roles -->
# NestledBurrow: roadmap и текущее исполнение

## Назначение

`ROADMAP.md` отвечает на два вопроса:

1. что мы делаем прямо сейчас;
2. что собираемся проверять следующим.

Состояние уже существующих механик и пользовательский verdict принадлежат `GAME.md`. Техническое доказательство реализации принадлежит коду, тестам и merged PR.

## Идентификаторы задач

Каждая согласованная работа получает постоянный номер до выдачи Codex prompt.

Канонический формат:

```text
Task #001 — Первая расчистка участка
```

- Номера назначаются последовательно и не переиспользуются.
- Один пользовательский результат сохраняет один Task number от prompt до merge и пользовательского verdict.
- Первая строка Codex prompt, branch, PR title, PR body и отчёты ролей сохраняют одну идентичность.
- Ветка: `task/<номер>-<slug>`.
- После открытия PR используется формат `PR #81 · Task #001 — Первая расчистка участка`.
- Base SHA не хранится здесь как долгоживущий факт: Лид проверяет актуальный `main` непосредственно перед запуском.

**Следующий свободный номер:** `Task #019`.

## Статусы

- `Запланировано` — направление согласовано, prompt ещё не выдан.
- `Готово к запуску` — самодостаточный prompt подготовлен.
- `В работе` — Codex выполняет задачу.
- `PR открыт` — результат доступен для приёмки.
- `Смёржено, ждёт теста` — код в `main`, пользователь ещё не дал verdict.
- `Принято` — пользователь подтвердил направление.
- `Переделать` — направление сохраняется, текущий результат не подходит.
- `Отклонено` — направление прекращено.
- `Завершено` — техническая или процессная работа завершена.
- `Заблокировано` — указан конкретный blocker.

## Сейчас

### Task #018 — Усилить сумерки и усталость

**Статус:** `Готово к запуску`

**Продуктовый вопрос:** становятся ли течение суток, добыча ресурсов и истощение выразительными, читаемыми и физически связными без резких визуальных, звуковых или двигательных переключений?

**Наблюдаемый результат:**

- обычное время идёт в темпе `1` реальная секунда = `1` игровая минута: полный игровой день длится `24` реальные минуты;
- default awake drain перенастроен с `7.2` на `9.6` реальной секунды за единицу энергии, поэтому полный запас `100` по-прежнему соответствует примерно `16` игровым часам бодрствования;
- дневной multiply-цвет нейтрален; вечером он проходит `#FFFFFF` → `#FFB380` с 17:00 до 18:00, `#FFB380` → `#FF648B` с 18:00 до 20:00 и `#FF648B` → `#425CD4` с 20:00 до 22:00;
- с 22:00 до 04:00 удерживается `#425CD4`; утром последовательность идёт строго обратно: `#425CD4` → `#FF648B` с 04:00 до 06:00, `#FF648B` → `#FFB380` с 06:00 до 08:00 и `#FFB380` → `#FFFFFF` с 08:00 до 09:00;
- переходы непрерывны на полуночи и всех фазовых границах, multiply применяется ко всему игровому viewport, а HUD остаётся читаемым поверх него;
- HUD сохраняет текст энергии и локализованные часы, но буквенные подписи древесины и рубинов заменяются уменьшенными примерно вдвое экземплярами тех же визуалов бревна и рубина, которые используются в мире; рядом остаются только числовые значения;
- каждый успешный удар по бревну воспроизводит короткий процедурный chiptune-impact, каждый успешный удар по рубиновой жиле — отдельный более звонкий процедурный mining SFX; звуки не требуют внешних файлов, учитывают `master` и `effects` volume и не играют при неуспешном interaction;
- при энергии не ниже 25% игрок сохраняет 100% скорости, при энергии 1% и ниже — 25%; между этими точками целевой multiplier равен `0.25 + 0.75 × (1 - (1 - x)²)`, где `x = clamp((energyFraction - 0.01) / 0.24, 0, 1)`, поэтому падение скорости ускоряется ближе к нулю;
- motor приходит к новому multiplier плавно, а текущая velocity не обрезается одномоментно при снижении энергии;
- после каждого успешного interaction-удара, который действительно потратил энергию и оставил меньше 15% запаса, трясётся только текст энергии по вертикали;
- low-energy feedback срабатывает для брёвен и рубиновых жил, но не от пассивного расхода, сна, debug-изменений или неуспешного interaction.

**Строки `GAME.md`:**

- `Спокойный непрерывный ритм без дневной отсечки`;
- `Суперприветливый UX при множестве систем`;
- `Перемещение, камера, keyboard/mobile input`;
- `RU/EN, HUD, audio и browser build`;
- `Developer/runtime tuning`;
- `Расчистка мусора, камней и брёвен`;
- `Энергия игрока`;
- `Ресурсы и инвентарь`;
- `Руда и редкие ресурсы`;
- `Непрерывное время, день/ночь и ускоренный сон`.

**Граница:** один законченный time/lighting/resource-HUD/interaction-feedback slice. Не добавлять погоду, календарь, источники света, постпроцессинг, новые ресурсы, инструменты, новые interaction-типы, экономику или внешние audio assets.

### Task #017 — Отключить Codex auto-repair

**Статус:** `Завершено`

`PR #110 · Task #017 — Отключить Codex auto-repair` смёржен в `main` коммитом `a6d5c6ebbd2bf0df1498972978d0af4d2d0e17f9`. Review-triggered auto-repair и автоматическое создание task PR удалены; обычный CI и ручной repair существующей PR-ветки сохранены.

### Task #016 — Проверить ручной цикл Codex и PR

**Статус:** `PR открыт`

`PR #109 · Task #016 — Проверить ручной цикл Codex и PR` открыт в `main` и содержит только disposable documentation smoke.

### Task #015 — Оживить сон, часы и суточный цикл

**Статус:** `Переделать`

`PR #106 · Task #015 — Оживить сон, часы и суточный цикл` смёржен в `main` коммитом `7bd4eb989ce49f5730ea5e858acb1287d272090d`.

Пользовательский тест подтвердил работающие часы, сон и общую симуляцию, но потребовал выразительную цветовую дугу сумерек и рассвета, темп `1` реальная секунда = `1` игровая минута, ресурсные иконки вместо буквенных подписей, процедурные chiptune-звуки добычи, нелинейное плавное замедление от низкой энергии и отдельную тряску текста энергии после затрат ниже 15%. Продолжение оформлено как `Task #018 — Усилить сумерки и усталость`.

**Канонический технический контракт:** `tasks/015-live-sleep-clock-cycle.md`.

**Продуктовый вопрос:** ощущается ли сон как реальное ускорение живого мира, а не как бегущие цифры, и помогают ли плавный световой цикл и аккуратные action prompts интуитивно читать состояние мира?

**Наблюдаемый результат:**

- после автоматического или ручного пробуждения игрок оказывается на свободной точке рядом с кроватью и сразу может двигаться;
- время отображается как часы без подписи: `HH:mm` для RU и `h:mm AM/PM` для EN;
- обычный темп равен `80` игровым секундам за одну реальную секунду: полный игровой день занимает ровно `18` реальных минут, один игровой час — `45` реальных секунд;
- default awake drain согласован с этим темпом: `1` энергия каждые `7.2` реальной секунды, чтобы полный запас `100` энергии по-прежнему длился примерно `16` игровых часов;
- сон сохраняет ускорение x8, но теперь ускоряет NPC и остальную игровую симуляцию, а не только счётчик;
- с 18:00 до 21:00 мир плавно темнеет, с 05:00 до 08:00 плавно светлеет, ночью остаётся читаемым;
- освещение реализовано плавным светло-синим multiply-тоном игрового мира без окрашивания HUD;
- на карте есть две постоянные рубиновые жилы, добываемые по тому же пятиударному и энергетическому правилу, что бревна;
- каждая жила выдаёт один рубин на последнем ударе, сохраняет remaining hits/cleared, переживает reload и восстанавливается через `NEW GAME`;
- HUD показывает энергию, древесину, рубины и локализованные часы без секундной подписи и без `x8`;
- desktop interaction prompt использует единый формат `SPACE · Действие`, где `SPACE` всегда прописными, а действие — sentence case;
- бревно показывает `SPACE · Рубить` / `SPACE · Chop`, touch-версия — только `Рубить` / `Chop`;
- кровать и рубины используют тот же шаблон: `SPACE · Спать`, `SPACE · Добыть`, `SPACE · Sleep`, `SPACE · Mine`.

**Строки `GAME.md`:**

- `Спокойный непрерывный ритм без дневной отсечки`;
- `Суперприветливый UX при множестве систем`;
- `NPC, диалоги и мини-квест`;
- `Save/reload и NEW GAME`;
- `RU/EN, HUD, audio и browser build`;
- `Developer/runtime tuning`;
- `Энергия игрока`;
- `Ресурсы и инвентарь`;
- `Руда и редкие ресурсы`;
- `Непрерывное время, день/ночь и ускоренный сон`.

**Граница:** это один законченный sleep/time/resource/interaction-UX slice. Не добавлять календарь, сезоны, погоду, освещение от отдельных ламп, полноценную рудную систему, инструменты, инвентарь или процедурное размещение ресурсов.

### Task #014 — Расширить цикл расчистки и сна

**Статус:** `Переделать`

`PR #105 · Task #014 — Расширить цикл расчистки и сна` смёржен в `main` коммитом `42f94a89a2921c56efe7eb7b180307aef7484395`.

Он добавил 43 бревна, пять ударов, расход энергии, awake drain, замедление при истощении, кровать, восстановление энергии и числовое ускорение времени x8. Пользовательский тест выявил два принципиальных дефекта: после сна игрок застревает в кровати, а ускорение сна не распространяется на NPC и игровой мир. Продолжение оформлено как самостоятельный расширенный результат `Task #015`.

### Task #001 — Первая расчистка участка

**Статус:** `Смёржено, ждёт теста`

`PR #86 · Task #001 — Первая расчистка участка` смёржен в `main` коммитом `87c7be151068dfb1144fdaaded3c35a8f817661d`.

### Task #011 — Make ui text crisp

**Статус:** `Смёржено, ждёт теста`

`PR #96 · Task #011 — Make ui text crisp` смёржен в `main` коммитом `d14798f0dccdd7b281c59b14074730882db627f4`.

## Служебная работа

### Task #006 — Стандартизировать идентичность задач

**Статус:** `Завершено`

`PR #85 · Task #006 — Стандартизировать идентичность задач` смёржен в `main`.

### Task #007 — Автоматизировать review и merge gates

**Статус:** `Отклонено`

`PR #87 · Task #007 — Автоматизировать review и merge gates` закрыт без merge.

### Task #008 — Pipeline smoke test

**Статус:** `Завершено`

`PR #92 · Task #008 — Pipeline smoke test` подтвердил базовый автоматический PR/CI/merge путь.

### Task #009 — Restore green CI

**Статус:** `Завершено`

`PR #93 · Task #009 — Restore green CI` восстановил обязательный check-chain.

### Task #010 — Clean pipeline smoke files

**Статус:** `Завершено`

`PR #95 · Task #010 — Clean pipeline smoke files` удалил временные smoke-файлы.

### Task #012 — Automate Codex review repair loop

**Статус:** `Переделать`

`PR #98 · Task #012 — Automate Codex review repair loop` установил первый self-hosted repair workflow, но первый end-to-end smoke не завершился.

### Task #013 — Restart Codex auto-repair cleanly

**Статус:** `Завершено`

`PR #102 · Task #013 — Restart Codex auto-repair cleanly` смёржен в `main` коммитом `c267a76784b5d2700d0a8e43fb3a2bc5955ed84f`.

**Process note:** `PR #103 · Task #014 — Prove Codex auto-repair end to end` смёржен в `main` коммитом `e67d6d4e8bcb14ad4ecf45945d4f958dfe55af39` с ошибочно повторно использованным `Task #014`. Это зафиксированная историческая аномалия; номер не должен использоваться повторно или переноситься на новую работу.

## Следом

### Task #002 — Полный цикл одного картофеля

`Запланировано`

Расчистить землю → вскопать → посадить → набрать воду → полить → ускоренно проспать → собрать урожай. Добавить только минимальные время, день/ночь, сон и восстановление энергии, необходимые этому циклу.

### Task #003 — Первый сервисный цикл таверны

`Запланировано`

Получить ингредиенты → приготовить один напиток → выставить его → один гость выбирает, потребляет, платит и уходит.

### Task #004 — Первый миньон

`Запланировано`

Оживить один исходный предмет → назначить одну полезную работу → показать заряд → после разряда вернуть его в исходный предмет. Без перезарядки и специализаций.

### Task #005 — Первая реакция аудитории на стиль таверны

`Запланировано`

Два маленьких профиля заведения с минимальной, но видимой разницей в привлечённых гостях. Только после работающего базового сервиса.

Порядок можно менять после пользовательских тестов. Номер и название уже заведённой задачи при перестановке не меняются.

## Уже сделано

- непрерывный top-down технический мир;
- движение, камера, коллизии, keyboard/mobile input;
- два NPC, диалоги и один мини-квест;
- versioned save/reload и `NEW GAME`;
- RU/EN, HUD, audio preferences и Pages-публикация;
- runtime movement и gameplay tuning;
- 43 persistent бревна с remaining hits;
- энергия, awake drain, истощение, кровать и черновой сон x8;
- каноническая продуктовая карта в `GAME.md`.

Это технический фундамент, а не доказательство готовности основной игры.

## Обновление

- Лид назначает следующий свободный Task number, обновляет раздел `Сейчас` и только затем выдаёт prompt.
- Codex сохраняет Task number и название в branch, PR title и PR body.
- Интегратор связывает PR и задачу, после merge обновляет `GAME.md` и статус здесь.
- После пользовательского теста Лид фиксирует verdict и конкретный вывод.
- Завершённая работа не остаётся бесконечно висеть активной.
- GitHub issues не являются обязательной памятью проекта.