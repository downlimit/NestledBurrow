{
  "name": "nestledburrow",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run check:input && npm run check:visual && npm run check:world && npm run build",
    "check:input": "node scripts/check-input.mjs",
    "check:visual": "node scripts/check-visual.mjs",
    "check:world": "node scripts/check-world.mjs"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
