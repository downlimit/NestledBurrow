{
  "name": "nestledburrow",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run check:input && npm run check:visual && npm run check:room-preview && npm run build",
    "check:input": "node scripts/check-input.mjs",
    "check:visual": "node scripts/check-visual.mjs",
    "check:room-preview": "python scripts/check-room-preview.py"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
